# Operator
# add(+), subtract(-), multiply(*), divide(/)
# exponents(**)

# str() function - convert the non-string values into an string
# if don't use it, Type Errors occurs
age = 25
str_message = "Happy " + str(age) + "th Birthday!"
print(str_message)